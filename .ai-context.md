# .ai-context.md

## Status
broken — git repo は存在するが初回コミット未。Next.js プロジェクト未初期化。

## Active Issues
- **#1 [Feature] プロジェクト初期化（Next.js + TypeScript）** ← 現在対応中
  - create-next-app (Pages Router, --no-app), src/ 構成, strict mode
  - vercel.json 追加, os-template.yml の project.name を CurtainCall へ更新
  - ボイラープレート削除
- #2 以降: ESLint/Vitest 設定、コア型定義、ゲームロジック実装... (合計38 issue)

## Decisions
- フレームワーク: Next.js + TypeScript（Pages Router、App Router 不使用）
  - 理由: 単一ページ state 管理のため
- デプロイ: Vercel
- ディレクトリ構成: src/ を採用
- TypeScript: strict mode 有効

## Next actions
1. Issue #1 実装（Next.js 初期化 → vercel.json → os-template.yml 更新 → ボイラープレート削除）
2. Issue #2: ESLint / Vitest 設定
3. Issue #9: コア型定義

## Known pitfalls
- create-next-app はバージョンによってデフォルト設定が変わる（App Router がデフォルトになっている）→ `--no-app` を明示すること
- os-template.yml の runtime.mode は host（Node.js ローカル実行のため）
- typecheck は tsc --noEmit を使う予定（Issue #2 で確定）

## Commands
- format: TODO（Issue #2 で設定）
- lint: TODO（Issue #2 で設定）
- typecheck: TODO（Issue #2 で設定）
- test: TODO（Issue #2 で設定）
- build: npm run build
- dev: npm run dev

## CI Notes
- CI は未設定（.github/workflows/ がまだない）
- Issue #1 では CI 設定は Out of Scope
